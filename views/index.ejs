<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quora Posts page</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Quora Posts</h1>
    <% for(post of posts){ %>
        <div>
            <h3 class="user">@<%= post.username %></h3>
            <img src="<%= post.img %>" class="img" alt="no image">
            <p class="content"><%= post.content %></p>
            <a href="http://localhost:8080/posts/<%= post.id %>" class="seeDetails">See Details</a>
            <a href="http://localhost:8080/posts/<%= post.id %>/edit" class="seeDetails">Edit Post</a>
            <form action="/posts/<%=post.id%>?_method=DELETE" method="post">
            <button class="btn">Delete Post</button>
            </form>
        </div>
        <% }%>

        <a href="http://localhost:8080/posts/new" class="addPost">Add New Post</a>
</body>
</html>